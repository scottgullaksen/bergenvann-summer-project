# So relative import are made possible

# In same directory as this file, run:
# $ pip install -e .

from setuptools import setup, find_packages

setup(name='myproject', version='1.0', packages=find_packages())